[pytest]
# Agrega la raíz del proyecto a sys.path para que los imports del tipo
# "from src.data.ingest import ..." funcionen sin instalar el paquete.
# Requiere pytest >= 7.0 (el proyecto usa 9.0.3).
pythonpath = .

# Directorio de tests y formato de salida
testpaths  = tests
addopts    = -v
